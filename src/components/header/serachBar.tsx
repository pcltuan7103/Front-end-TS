import { Search } from "lucide-react";
import React from "react";

const SearchBar = () => {
    return (
        <div>
            <Search className="w-5 h-5 hover:text-shop-light_blue hoverEffect" />
        </div>
    );
};

export default SearchBar;
